import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  expect(2).toBe(2); // Comparação simples para garantir que o teste sempre pass
});
